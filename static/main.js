// static/main.js

// This can be expanded later for dynamic refresh, warning tones, etc.
console.log("Dashboard script loaded.");

// Optional: reload every 60s (already inlined in HTML too)
setTimeout(() => location.reload(), 60000);
